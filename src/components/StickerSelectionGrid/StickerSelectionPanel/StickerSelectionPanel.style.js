import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 16,
        paddingTop: 0,
        width: '100%'
    },
    paragraph: {
        fontSize: 16,
        textAlign: 'center'
    }
});

export default styles;
